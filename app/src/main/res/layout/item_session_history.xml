<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Minimal placeholders for SessionHistoryAdapter ViewBinding -->
    <TextView android:id="@+id/sessionDate" android:layout_width="0dp" android:layout_height="0dp" />
    <TextView android:id="@+id/sessionDuration" android:layout_width="0dp" android:layout_height="0dp" />
    <TextView android:id="@+id/qualityScore" android:layout_width="0dp" android:layout_height="0dp" />
    <TextView android:id="@+id/efficiencyScore" android:layout_width="0dp" android:layout_height="0dp" />
    <ImageView android:id="@+id/statusIcon" android:layout_width="0dp" android:layout_height="0dp" />
    <TextView android:id="@+id/movementLevel" android:layout_width="0dp" android:layout_height="0dp" />
    <TextView android:id="@+id/noiseLevel" android:layout_width="0dp" android:layout_height="0dp" />
    <View android:id="@+id/sessionCard" android:layout_width="0dp" android:layout_height="0dp" />

    <!-- Actual minimal visible content -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Session Item"
        android:gravity="center"
        android:background="@color/surface_dark"
        android:textColor="@color/text_primary" />

</LinearLayout>